# O operador ternário é uma forma de simplificar o IF e ELSE

# Declaração de Variáveis

nome = input("Digite seu nome: ")
idade = int(input("Digite sua idade: "))

result = "é maior de idade." if idade >= 18 else "é menor de idade."

# Imprime o resultado

print(nome, result)
